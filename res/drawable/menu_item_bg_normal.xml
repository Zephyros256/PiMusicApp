<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <!-- TODO start, center en endColor aanpassen naar wens -->
    <gradient
            android:startColor="@color/menu_bg_start"
            android:centerColor="@color/menu_bg_end"
            android:endColor="@color/menu_bg_start"
            android:angle="90" />
</shape>